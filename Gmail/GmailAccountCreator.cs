namespace Gmail
{
    public class GmailAccountCreator{

        private string nombre {  get; set; }
        private string apellido { get; set; }
        // fecha_nacimiento;
        private List<string> sexo { get; set; }


        // Validar nombre [Longitud, caracteres especiales]
        // Validar campos vacios
        // Validar fecha [Numeros, rango de fechas [No puede ser el mismo dia por la edad]]
        // Validar genero
        // Validar correo [Longitud (Entre 6 y 30 caracteres, Caracteres especiales (Letras, números y signos de puntuación))]
        // Validar contraseña [Caracteres especiales, mayusculas y minusculas, Campo 'Confirmar contraseña' igual, Longitud (mayor a 8), contraseñas sencillas]
        // Número de telefono [Lada (ladas que existan), Longitud de número (10 números), NO CADENAS]
        // Código de verificacion





    }
}
